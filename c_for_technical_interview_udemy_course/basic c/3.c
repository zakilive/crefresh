#include<stdio.h>
#include<stdlib.h>
int main()
{
    int x;
    int avar;
    scanf("%d",&avar);
    x=avar+1;
    printf("the value is: %d and %d\n",avar,x);
    return (EXIT_SUCCESS); //it's known as macro
}
