//contains some function definitions


/*
function to calculate area of a circle and returns the result to the caller. The function receives the raius of the circle as input parameter.
*/

double getcircleArea(double radius){
    double area;
    area=3.14*rad*rad;
    return area;
}

/*
function to calculate area of triangle and returns the result to the caller. This function receives the base and the height of a triangle as a input parameter
*/

*/


double getTriangleArea(double base,double height)
{
    double area;
    area=0.5*base*height;
    return area;
}
