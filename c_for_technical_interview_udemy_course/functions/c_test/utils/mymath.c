/*
function that finds the simple interest in supplying principal, rate of interest and time.
*/

double getSimpleInterest(double principal,double rate,double time)
{
    double interest;
    interest=(principal*rate*time);
    return interest;
}
